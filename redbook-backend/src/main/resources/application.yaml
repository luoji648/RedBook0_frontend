server:
  port: 8081
spring:
  application:
    name: redbook-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/redbook?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  data:
    redis:
      client-type: lettuce  # ? jedis???? lettuce
      host: 192.168.220.128
      port: 6379
      password: 123321
      database: 0
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1
          time-between-eviction-runs: 10s
  rabbitmq:
    host: 192.168.220.128 # ?????IP
    port: 5672 # ??
    virtual-host: /hm-dp # ????
    username: root # ???
    password: 123321 # ??
    listener:
      simple:
        prefetch: 5 # # ??????????5?????????????????
        acknowledge-mode: auto # ??ack  ?????????????ack.  ?????????????????????
        retry:
          enabled: true # ?????????
          initial-interval: 1000ms # ??????????1?
          multiplier: 1 # ???????????????? = multiplier * last-interval???csma-cd?
          max-attempts: 3 # ??????
          stateless: false # true????false??????????????????false
#  jackson:
#    default-property-inclusion: non_null # JSON?????????
mybatis-plus:
  type-aliases-package: com.hmdp.entity # ?????
# springdoc-openapi????
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      # ????????????
      packages-to-scan: com.hmdp.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn
dify:
  url: http://localhost/v1/chat-messages
  key:
    test: app-JI83dLRHlJqjukW9zAGkdp8v
logging:
  level:
    com.hmdp: debug